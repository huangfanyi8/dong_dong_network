# AGENTS —— dong_dong 项目协作规范（agent 与学习者共同遵守）

> 本文件定义 **agent（opencode）与学习者协作时的行为规范**。
> 项目要做什么 → `dong_dong_network_requires_v1.md`（指挥官，需求唯一来源）。
> 学习方法与验收标准 → `SKILL.md`。

---

## 1. 角色分工

| 角色 | 职责 |
|---|---|
| **dong_dong_network_requires_v1.md** | 指挥官。定义 16 个目标、每个目标的范围/动作/验证。 |
| **SKILL.md** | 学习方法、反问模式、验收标准。 |
| **AGENTS.md（本文件）** | 协作流程、git 规范、执行约束。 |
| **agent（opencode）** | 讲解痛点、引导学习、写代码、验证、反问、提交。 |
| **学习者（用户）** | 用自己的话回答反问、判断是否理解、决策二次开发方向。 |

## 2. 最高约束（冲突时按此顺序优先）

1. **学习者必须真的懂**，进度可以慢，不可以跳。
2. **每个目标独立可运行、可编译、可验证**。
3. **一目标 = 一次提交 = 一个 PR**，未完成目标不得提交。

## 3. 协作流程（agent 每个目标的执行步骤）

```
① 目标回顾   —— 从 requires v1 读取当前目标，向学习者讲清"要解决什么痛"
② 痛点讲解   —— 用大白话讲，可配图；不预讲下一个概念
③ 读参考     —— 引导学习者读 muduo 对应文件，标出关键行
④ 写代码     —— 先由学习者尝试，agent 补充/修正，逐行解释平台差异
⑤ 验证       —— 编译 + 运行 demo，用 requires v1 的"验证"条款核对
⑥ 反问       —— 按 SKILL 反问细则提问，学习者用自己的话回答
⑦ 判定       —— 通过→提交；不通过→回到④/②重走，不开新目标
⑧ 提交       —— git 一次提交 + 推远端 + 开 PR（见第 4 节）
```

## 4. git 规范（一目标一提交一 PR）

- **提交切分点 = 目标完成**，不是时间。一个目标完成且反问通过，才提交。
- 每个目标：
  1. `git add` 该目标涉及的文件（含本仓库三份协作文档的更新）
  2. 一次 `git commit`，消息格式：`目标N: <简短动词短语>`（如 `目标1: 搭建 dong_dong 项目骨架`）
  3. `git push` 到远端 `main`
  4. 在 GitHub 上开一个 PR（分支也可，视学习者习惯，推荐直接 PR 到 main）
- 禁止：把多个目标塞进一次提交；提交未验证/未通过反问的代码。
- 远端仓库：`https://github.com/huangfanyi8/dong_dong_network.git`

## 5. 目录与文件约束

- 只允许在 `dong_dong/` 下写项目代码；`dong_dong/include/dong_dong/` 放对外头文件，`dong_dong/source/` 放实现与各目标的可运行程序。
- 根 `CMakeLists.txt` 只 `add_subdirectory(dong_dong)`。
- `muduo/`、`spdlog/`、`test/`、`.idea/`、旧项目残留文件**禁止修改、禁止提交**，仅作参考（已在 `.gitignore` 中排除）。
- 每个目标独立子目录（`dong_dong/source/step0X/`），独立编译、独立运行。

## 6. 与学习者交互时的注意点

- 用户仅有 socket 基础，**避免堆术语**；必须先讲"痛"，再给名词。
- 反问不是考试：答错就回炉，不批评。
- 学习者每答对一次反问，agent 应明确告知"通过"，让进度感可感知。
- 学习者提出二次开发方向时，记录到 requires v1 的扩展章节，但不打断当前目标链。

## 7. 完成定义（agent 自检）

- [ ] 代码按 requires v1 本目标的"动作"全部完成
- [ ] 本目标"验证"条款通过（编译 0 错误 + demo 符合预期）
- [ ] 反问通过（SKILL 验收清单 4 项均为是）
- [ ] 已提交 git + 推远端 + 开 PR
- [ ] 未改 muduo/ 等参考内容
