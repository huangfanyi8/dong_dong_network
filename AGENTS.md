# AGENTS.md —— dong_dong 网络库项目规则（工程行为与边界）

> 本文件定义 Agent 在本仓库中的**工程行为与边界**。
> 项目要做什么、每个目标的验证标准 → `dong_dong_network_requires_v1.md`。
> 如何讲解、如何做理解验收 → `.opencode/skills/dong-dong-learning/SKILL.md`。

---

## 1. 文档职责与优先级

### 1.1 requires v1（需求基线）
负责定义：项目目标、学习路线、当前目标的范围与内容、**功能验证标准**、项目进度。它是项目需求唯一来源，但不是对用户当前明确指令的限制。

### 1.2 学习技能（dong-dong-learning）
负责定义：问题驱动讲解方式、代码辅助方式、muduo 对照方式、**理解验收方式**。

### 1.3 当前用户指令
用户当前明确提出的需求**优先于**文档默认工作方式。例如：用户要求直接实现代码则不强制先编码；要求只分析则不改码；要求跳过本轮反问则可以跳过，但不代表删除验收标准。

---

## 2. 项目定位与目录边界

- `dong_dong` 是从裸 socket 逐步构建的跨平台 C++ 网络库（Windows + Linux）。
- 项目代码只写于 `dong_dong/`：`include/dong_dong/` 对外头文件，`source/` 实现与可运行程序。
- `spdlog/` 作为日志库参与构建（第三方依赖，不修改其源码）。
- 根 `CMakeLists.txt` 只 `add_subdirectory(dong_dong)`。
- `muduo/`、`test/` 不参与构建、**禁止修改**，仅作参考。

---

## 3. 代码修改边界

- **允许**：`dong_dong/`；与当前目标直接相关的项目配置文件；用户明确指定的文件。
- **禁止**：`muduo/`、`test/`；`spdlog/` 源码（作为依赖使用，不改其源码）；与当前目标无关的业务代码或基础设施。
- **原则**：小步修改；保持接口稳定；不做与当前目标无关的大规模重构；不为了"像 muduo"主动添加抽象。

---

## 4. 正确性优先

涉及代码修改或 review 时，优先检查：编译正确性、socket 生命周期、资源所有权、内存管理、并发安全、异常安全、阻塞/非阻塞语义、错误处理、fd/socket 状态、Windows/Linux 平台差异。在正确性未确认前，不以性能优化或代码美化作为主要目标。

---

## 5. 构建与运行验证

- 默认构建：`cmake -B build && cmake --build build`。
- 涉及运行时行为时，必须运行对应 demo。
- 验证失败：先定位实际失败位置，判断是代码/构建/平台/环境问题，最小化修改，修复后重新编译运行。
- 不得只根据"代码看起来没问题"判定目标完成。

---

## 6. 目标完成判定

- **功能完成**标准由 requires v1 定义；**理解验收**方式由 SKILL 定义。
- 两者都**不等同于 Git 操作**：commit / push / PR 不构成目标完成条件。
- Agent 不应因学习者暂时未通过反问，就擅自修改需求或跳到下一目标。

---

## 7. Git 规则

Git 操作服从用户当前要求。默认建议：

- 一个完整目标对应一个逻辑提交；
- 提交消息格式：`目标NN: <简短动词短语>`（NN 与 requires v1 编号一致，如 `目标01`）。

除非用户明确要求，Agent 不自动执行 `git push`、创建/合并 PR、修改远端 `main`。

---

## 8. 外部文件加载

- 开始任何目标前：用 Read 读取 requires v1 的当前目标章节，以它为唯一需求来源。
- 进行反问/验收时：加载技能 `dong-dong-learning`。
- 按需加载，不必预载全部。
